// io/sstr1.cpp
#include <iostream>
#include <sstream>
#include <bitset>
using namespace std;

int main()
{
    ostringstream os;

    // ƒес€тичное и шестнадцатеричное значение
    os << "dec: " << 15 << hex << " hex: " << 15 << endl;
    cout << os.str() << endl;

    // ѕрисоединение вещественного числа и битового пол€
    bitset<15> b(5789);
    os << "float: " << 4.67 << " bitset: " << b << endl;
    // ѕерезапись восьмеричным числом
    os.seekp(0);
    os << "oct: " << oct << 15;
    cout << os.str() << endl;
}


